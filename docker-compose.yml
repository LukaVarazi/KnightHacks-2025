services:
  frontend:
    build:
      context: frontend
      dockerfile: Dockerfile
    ports:
      - "9998:80"
    restart: always

  backend:
    build:
      context: back-end
      dockerfile: Dockerfile
    ports:
      - "9999:80"
    restart: always

  agent:
    build:
      context: back-end
      dockerfile: Dockerfile
    restart: always
    command: ["python", "-m", "adk", "api_server"]
